meta {
  name: Add admin response
  type: http
  seq: 3
}

patch {
  url: {{baseUrl}}/reports/{{reportId}}
  body: json
  auth: inherit
}

body:json {
  {
    "adminResponse": "Reviewed and dismissed"
  }
}
