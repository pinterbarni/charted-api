// const env = process.env.NODE_ENV ?? 'development';

// if (env === 'production') {
//   await runProductionSeed();
// } else if (env === 'staging') {
//   await runStagingSeed();
// } else {
//   await runDevelopmentSeed();
// }
